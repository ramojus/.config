-- haskell.plugin.rename.config.crossModule": true
local opts = {
    settings = {
        haskell = {
            plugin = {
                rename = {
                    config = {
                        crossModule = true,
                    }
                }
            }
        }
    }
}

return opts

